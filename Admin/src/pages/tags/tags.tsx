import { defineComponent, toRefs, toRaw, onMounted } from "vue";
export default defineComponent({
    setup() {
        return () => (
            <el-row>
                <el-row>
                    <h1>TSX</h1>
                </el-row>
            </el-row>
        )
    }
})